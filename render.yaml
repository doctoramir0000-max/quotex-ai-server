services:
  - type: web
    name: quotex-ai-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn kronos_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
